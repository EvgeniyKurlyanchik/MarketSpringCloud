server:
  port: 8111
  servlet:
    context-path: /users
spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:file:./userss;MODE=PostgreSQL
    username: sa
    password:
  jpa:
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
  h2:
    console:
      enabled: true
      settings:
        web-allow-others: false
